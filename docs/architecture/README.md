# Architecture Documentation

Use these documents in this order:

1. `system-overview.md` for boundaries and package responsibilities.
2. `runtime-topology.md` for deploy/runtime concerns and service graph.
3. `domain-model.md` for core entities and relationships.
4. `sync-and-events.md` for event flow, topics, offsets, and snapshot behavior.

For decision history, see ADRs in `docs/adr/`.
