## Related repositories / libraries

This repository uses some custom libraries, notably:
- `@schnau/effect-drizzle`: ~/urbs/Libraries/effect-drizzle
- `@groundswell/*`: ~/urbs/Libraries/Groundswell

Feel free to read those repositories for more information, edit them, or even contribute to them and release them whenever you need to. This repository is currently the only consumer of these libraries, so you can safely modify them without worrying about breaking other consumers.

## Skill usage

When you need docs, reference or examples on specific OSS libraries, you can use the Context Repo skill to fetch and explore them.

## Subagents

Use subagents for large, complex tasks or executing plans whenever the task at hand is too complex to be handled by a single agent and can be broken down into smaller, more manageable tasks. You manage the backlog, the plan execution, orchestrate, delegate and review the subagents' work. 
