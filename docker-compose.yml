services:
  db:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: stu
      POSTGRES_USER: stu
      POSTGRES_DB: stu

  kafka:
    image: apache/kafka
    restart: always
    ports:
      - "9092:9092"
