name: "studienbuch"

services:
  database:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: stu
      POSTGRES_USER: stu
      POSTGRES_DB: stu
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "stu", "-d", "stu"]
      interval: 1s
      timeout: 5s
      retries: 60
    volumes:
      - postgres_data:/var/lib/postgresql/data

  event-stream:
    image: rabbitmq:4-management-alpine
    restart: unless-stopped
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    configs:
      - source: enabled_plugins
        target: /etc/rabbitmq/enabled_plugins
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "is_running"]
      interval: 10s
      timeout: 5s
      retries: 60

  api:
    profiles: [live]
    image: studienbuch-api:nix
    pull_policy: never
    restart: unless-stopped
    depends_on:
      database:
        condition: service_healthy
      event-stream:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
    environment:
      MANAGEMENT_DATABASE_URL: postgresql://stu:stu@database:5432/stu
      PULSAR_URL: rabbitmq-stream://event-stream:5552
      API_PORT: 80
      LINEAR_API_KEY: ${LINEAR_API_KEY:?LINEAR_API_KEY is required}
      NEXT_PUBLIC_AXIOM_DATASET: ${NEXT_PUBLIC_AXIOM_DATASET:?NEXT_PUBLIC_AXIOM_DATASET is required}
      NEXT_PUBLIC_AXIOM_TOKEN: ${NEXT_PUBLIC_AXIOM_TOKEN:?NEXT_PUBLIC_AXIOM_TOKEN is required}
      CACHE_DIR: /cache
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://localhost/healthz').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
    volumes:
      - cache:/cache

  nextjs:
    profiles: [live]
    image: studienbuch-nextjs:nix
    pull_policy: never
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
      database:
        condition: service_healthy
      event-stream:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
    environment:
      NODE_ENV: production
      HOSTNAME: 0.0.0.0
      PORT: 80
      BASE_URL: ${NEXTJS_BASE_URL:-http://localhost:3000}
      NEXT_PUBLIC_DEPLOYMENT_ENV: ${NEXT_PUBLIC_DEPLOYMENT_ENV:-dev}
      MANAGEMENT_DATABASE_URL: postgresql://stu:stu@database:5432/stu
      PULSAR_URL: rabbitmq-stream://event-stream:5552
      API_PORT: 80
      LINEAR_API_KEY: ${LINEAR_API_KEY:?LINEAR_API_KEY is required}
      NEXT_PUBLIC_AXIOM_DATASET: ${NEXT_PUBLIC_AXIOM_DATASET:?NEXT_PUBLIC_AXIOM_DATASET is required}
      NEXT_PUBLIC_AXIOM_TOKEN: ${NEXT_PUBLIC_AXIOM_TOKEN:?NEXT_PUBLIC_AXIOM_TOKEN is required}
      CACHE_DIR: /cache
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1/').then((r)=>process.exit(r.status < 400 ? 0 : 1)).catch(()=>process.exit(1))",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
    volumes:
      - cache:/cache

  admin-panel:
    profiles: [live]
    image: studienbuch-admin-panel:nix
    pull_policy: never
    restart: unless-stopped
    depends_on:
      database:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
    environment:
      NODE_ENV: production
      PORT: 80
      MANAGEMENT_DATABASE_URL: postgresql://stu:stu@database:5432/stu
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://localhost/').then((r)=>process.exit(r.status < 400 ? 0 : 1)).catch(()=>process.exit(1))",
        ]
      interval: 10s
      timeout: 5s
      retries: 12

  console-cron:
    profiles: [live]
    image: studienbuch-console-cron:nix
    pull_policy: never
    depends_on:
      api:
        condition: service_healthy
      database:
        condition: service_healthy
      event-stream:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
      legacy-database:
        condition: service_healthy
    init: true
    tty: true
    environment:
      API_PORT: 80
      LINEAR_API_KEY: ${LINEAR_API_KEY:?LINEAR_API_KEY is required}
      NEXT_PUBLIC_AXIOM_DATASET: ${NEXT_PUBLIC_AXIOM_DATASET:?NEXT_PUBLIC_AXIOM_DATASET is required}
      NEXT_PUBLIC_AXIOM_TOKEN: ${NEXT_PUBLIC_AXIOM_TOKEN:?NEXT_PUBLIC_AXIOM_TOKEN is required}
      PULSAR_URL: rabbitmq-stream://event-stream:5552
      MANAGEMENT_DATABASE_URL: postgresql://stu:stu@database:5432/stu
      LEGACY_DATABASE_URL: postgresql://postgres:postgres@legacy-database:5432/postgres
      UNTIS_KADMOS_NAME: ${UNTIS_KADMOS_NAME:?UNTIS_KADMOS_NAME is required}
      UNTIS_KADMOS_USERNAME: ${UNTIS_KADMOS_USERNAME:?UNTIS_KADMOS_USERNAME is required}
      UNTIS_KADMOS_PASSWORD: ${UNTIS_KADMOS_PASSWORD:?UNTIS_KADMOS_PASSWORD is required}
      CACHE_DIR: /cache
    volumes:
      - cache:/cache
    restart: unless-stopped
    configs:
      - source: crontab
        target: /var/spool/cron/crontabs/root

  migrations:
    # profiles: [live]
    image: studienbuch-migrations:nix
    pull_policy: never
    environment:
      MANAGEMENT_DATABASE_URL: postgresql://stu:stu@database:5432/stu
    depends_on:
      database:
        condition: service_healthy
    restart: no

  legacy-database:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres", "-d", "postgres"]
      interval: 1s
      timeout: 5s
      retries: 60
    volumes:
      - postgres_legacy_data:/var/lib/postgresql/data

configs:
  crontab:
    content: |
      */10 * * * * console pull --school=igs-lil
      @reboot console pull --school=igs-lil

  enabled_plugins:
    content: |
      [rabbitmq_stream,rabbitmq_stream_management].

volumes:
  postgres_data:
  cache:
  rabbitmq_data:
  postgres_legacy_data:
