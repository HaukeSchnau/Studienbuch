{
  "name": "@acme/db",
  "version": "0.1.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "db:push-delete": "prisma db push --skip-generate --force-reset --accept-data-loss",
    "db": "prisma",
    "dev": "BROWSER=none prisma studio --port 5556"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "typescript": "^4.9.5",
    "zod-prisma": "^0.5.4"
  }
}
