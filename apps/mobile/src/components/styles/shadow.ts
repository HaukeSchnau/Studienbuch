import { StyleSheet } from "react-native";

export const { shadow } = StyleSheet.create({
  shadow: {
    shadowColor: "#000",
    shadowOffset: {
      width: 4,
      height: 4,
    },
    shadowOpacity: 0.16,
    shadowRadius: 12,
    elevation: 5,
  },
});
