{
  "name": "@acme/flutter",
  "scripts": {
    "install": "flutter pub get",
    "flutter:gen": "flutter pub run build_runner clean && flutter pub run build_runner build --delete-conflicting-outputs"
  }
}
